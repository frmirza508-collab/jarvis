{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: only the JARVIS core connection command is exposed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
